import { CheckoutModelItem } from './checkout-model-item';

describe('CheckoutModelItem', () => {
  it('should create an instance', () => {
    expect(new CheckoutModelItem()).toBeTruthy();
  });
});
